# RF09 e RF10 - Gerenciamento de Conversas e Auditoria

---

## 1. Resumo dos Requisitos

### RF09 - Gerenciamento de Conversas

> - Suporte a conversas do tipo `private` (chat privado) e `group` (grupo).
> - Registro de membros da conversa com papéis: `owner`, `admin`, `member`.
> - Rastrear última leitura (`last_read_at`) por usuário em cada conversa.

### RF10 - Auditoria e Logs

> - Gerar logs de auditoria para operações do sistema.
> - Registrar: evento, tipo de entidade, ID da entidade, usuário, detalhes e timestamp.

### Importância no Contexto do Sistema

**RF09** implementa o modelo de domínio de conversas, diferenciando:
- **Private**: Exatamente 2 membros, sem nome
- **Group**: N membros, com nome e hierarquia de papéis

**RF10** garante **rastreabilidade** — fundamental para compliance, debugging e segurança. Toda operação crítica deixa rastro no banco.

---

## 2. Solução Arquitetural

### 2.1 Modelo de Dados de Conversas

```sql
┌─────────────────────────────────────────────────────────────┐
│                      conversations                          │
├─────────────────────────────────────────────────────────────┤
│ conversation_id    │ UUID PK                                │
│ type               │ 'private' | 'group'                    │
│ name               │ VARCHAR (NULL para private)            │
│ created_by         │ UUID FK users                          │
│ last_message_*     │ Desnormalização para performance       │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 1:N
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  conversation_members                        │
├─────────────────────────────────────────────────────────────┤
│ conversation_id    │ UUID FK                                │
│ user_id            │ UUID FK                                │
│ role               │ 'owner' | 'admin' | 'member'           │
│ joined_at          │ TIMESTAMP                              │
│ last_read_at       │ TIMESTAMP (tracking de leitura)        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Arquitetura de Auditoria

```
┌─────────────────┐
│   Qualquer      │
│   Operação      │
│   Crítica       │
└────────┬────────┘
         │
         │ insertAuditLog()
         ▼
┌─────────────────────────────────────────────────────────────┐
│                       audit_logs                             │
├─────────────────────────────────────────────────────────────┤
│ log_id         │ UUID PK                                    │
│ event_type     │ 'user.created', 'message.sent', etc.       │
│ entity_type    │ 'user', 'message', 'file', 'conversation'  │
│ entity_id      │ UUID (referência genérica)                 │
│ user_id        │ UUID FK (quem executou)                    │
│ details        │ JSONB (contexto adicional)                 │
│ created_at     │ TIMESTAMP                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Implementação

### 3.1 RF09 - Gerenciamento de Conversas

#### 3.1.1 Schema do Banco (`init-db.sql`)

**Linhas 30-65**:
```sql
-- Conversas
CREATE TABLE IF NOT EXISTS conversations (
    conversation_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    type VARCHAR(20) NOT NULL CHECK (type IN ('private', 'group')),
    name VARCHAR(255), -- Nome do grupo (NULL para privadas)
    created_by UUID NOT NULL REFERENCES users(user_id),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    
    -- Desnormalização: última mensagem para listagem rápida
    last_message_id UUID,
    last_message_at TIMESTAMP,
    last_message_snippet TEXT,
    
    is_active BOOLEAN DEFAULT true
);

-- Membros da conversa
CREATE TABLE IF NOT EXISTS conversation_members (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    conversation_id UUID NOT NULL REFERENCES conversations(conversation_id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,
    role VARCHAR(20) DEFAULT 'member' CHECK (role IN ('owner', 'admin', 'member')),
    joined_at TIMESTAMP DEFAULT NOW(),
    last_read_at TIMESTAMP,  -- Tracking de leitura por usuário
    
    UNIQUE(conversation_id, user_id)
);

CREATE INDEX idx_conversation_members_conv ON conversation_members(conversation_id);
CREATE INDEX idx_conversation_members_user ON conversation_members(user_id);
```

#### 3.1.2 Criação de Conversa Privada (`ConversationService.php`)

**Linhas 27-78**:
```php
public function CreatePrivateConversation(CreatePrivateConversationRequest $request): CreateConversationResponse
{
    $response = new CreateConversationResponse();
    
    try {
        $userId = $request->getUserId();
        $otherUserId = $request->getOtherUserId();
        
        // Validação: não pode conversar consigo mesmo
        if ($userId === $otherUserId) {
            throw new \Exception("Cannot create conversation with yourself");
        }
        
        // Buscar outro usuário (por UUID, email ou phone)
        $otherUser = $this->findUser($otherUserId);
        if (!$otherUser) {
            throw new \Exception("User not found");
        }
        $otherUserId = $otherUser['user_id'];
        
        // IDEMPOTÊNCIA: verificar se já existe
        $existingId = $this->database->checkPrivateConversationExists($userId, $otherUserId);
        
        if ($existingId) {
            $conversationData = $this->database->getConversationById($existingId);
            $response->setMessage("Conversation already exists");
        } else {
            // Criar conversa tipo 'private' (sem nome)
            $conversationId = $this->database->createConversation('private', null, $userId);
            
            // Adicionar membros com roles
            $this->database->addConversationMember($conversationId, $userId, 'owner');
            $this->database->addConversationMember($conversationId, $otherUserId, 'member');
            
            $conversationData = $this->database->getConversationById($conversationId);
            $response->setMessage("Conversation created successfully");
        }
        
        $response->setSuccess(true);
        $response->setConversation($this->mapConversation($conversationData));
        
    } catch (\Exception $e) {
        $this->logger->error("CreatePrivateConversation error: " . $e->getMessage());
        $response->setSuccess(false);
        $response->setMessage($e->getMessage());
    }
    
    return $response;
}
```

#### 3.1.3 Verificação de Conversa Existente (`Database.php`)

```php
public function checkPrivateConversationExists(string $userId1, string $userId2): ?string
{
    // Busca conversa private onde AMBOS usuários são membros
    $stmt = $this->pdo->prepare('
        SELECT c.conversation_id
        FROM conversations c
        JOIN conversation_members cm1 ON c.conversation_id = cm1.conversation_id
        JOIN conversation_members cm2 ON c.conversation_id = cm2.conversation_id
        WHERE c.type = :type
        AND cm1.user_id = :user1
        AND cm2.user_id = :user2
        AND c.is_active = true
        LIMIT 1
    ');
    
    $stmt->execute([
        'type' => 'private',
        'user1' => $userId1,
        'user2' => $userId2
    ]);
    
    $result = $stmt->fetch();
    return $result ? $result['conversation_id'] : null;
}
```

#### 3.1.4 Criação de Grupo (`ConversationService.php`)

**Linhas 80-130**:
```php
public function CreateGroup(CreateGroupRequest $request): CreateConversationResponse
{
    $response = new CreateConversationResponse();
    
    try {
        $userId = $request->getUserId();
        $groupName = $request->getGroupName();
        $memberIds = $request->getMemberUserIds();
        
        if (!$groupName) {
            throw new \Exception("Group name is required");
        }
        
        // Criar conversa tipo 'group' COM nome
        $conversationId = $this->database->createConversation('group', $groupName, $userId);
        
        // Criador é OWNER
        $this->database->addConversationMember($conversationId, $userId, 'owner');
        
        // Adicionar outros membros
        foreach ($memberIds as $memberId) {
            $memberUser = $this->findUser($memberId);
            
            if ($memberUser) {
                // Novos membros são 'member' por padrão
                $this->database->addConversationMember(
                    $conversationId, 
                    $memberUser['user_id'], 
                    'member'
                );
            }
        }
        
        $conversationData = $this->database->getConversationById($conversationId);
        
        $response->setSuccess(true);
        $response->setMessage("Group created successfully");
        $response->setConversation($this->mapConversation($conversationData));
        
    } catch (\Exception $e) {
        $this->logger->error("CreateGroup error: " . $e->getMessage());
        $response->setSuccess(false);
        $response->setMessage($e->getMessage());
    }
    
    return $response;
}
```

#### 3.1.5 Tracking de Leitura (`Database.php`)

```php
public function updateLastReadAt(string $conversationId, string $userId): void
{
    $stmt = $this->pdo->prepare('
        UPDATE conversation_members
        SET last_read_at = NOW()
        WHERE conversation_id = :conversation_id
        AND user_id = :user_id
    ');
    
    $stmt->execute([
        'conversation_id' => $conversationId,
        'user_id' => $userId
    ]);
}

// Usado para calcular unread count
public function getUnreadCount(string $conversationId, string $userId): int
{
    $stmt = $this->pdo->prepare('
        SELECT COUNT(*) as count
        FROM messages m
        JOIN conversation_members cm ON m.conversation_id = cm.conversation_id
        WHERE m.conversation_id = :conversation_id
        AND cm.user_id = :user_id
        AND m.created_at > COALESCE(cm.last_read_at, \'1970-01-01\')
        AND m.from_user_id != :user_id
    ');
    
    $stmt->execute([
        'conversation_id' => $conversationId,
        'user_id' => $userId
    ]);
    
    return (int)$stmt->fetch()['count'];
}
```

#### 3.1.6 Listagem de Conversas (`ConversationService.php`)

**Linhas 132-190**:
```php
public function ListConversations(ListConversationsRequest $request): ListConversationsResponse
{
    $response = new ListConversationsResponse();
    
    try {
        $userId = $request->getUserId();
        $limit = $request->getLimit() ?: 50;
        
        $conversationsData = $this->database->getUserConversations($userId, $limit);
        
        $conversations = [];
        foreach ($conversationsData as $data) {
            $summary = new ConversationSummary();
            $summary->setConversationId($data['conversation_id']);
            $summary->setType($data['type']);
            
            // Para private: exibir nome do outro usuário
            if ($data['type'] === 'private') {
                $fullConv = $this->database->getConversationById($data['conversation_id']);
                foreach ($fullConv['members'] as $member) {
                    if ($member['user_id'] !== $userId) {
                        $summary->setName($member['username']);
                        break;
                    }
                }
            } else {
                // Para groups: usar nome do grupo
                $fullConv = $this->database->getConversationById($data['conversation_id']);
                $summary->setName($fullConv['name']);
            }
            
            $summary->setLastMessage($data['last_message_snippet'] ?? '');
            $summary->setLastMessageAt($data['last_message_at'] ?? '');
            
            $conversations[] = $summary;
        }
        
        $response->setSuccess(true);
        $response->setConversations($conversations);
        
    } catch (\Exception $e) {
        $this->logger->error("ListConversations error: " . $e->getMessage());
        $response->setSuccess(false);
    }
    
    return $response;
}
```

---

### 3.2 RF10 - Auditoria e Logs

#### 3.2.1 Schema de Auditoria (`init-db.sql`)

**Linhas 104-120**:
```sql
CREATE TABLE IF NOT EXISTS audit_logs (
    log_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    event_type VARCHAR(50) NOT NULL,       -- 'user.created', 'message.sent'
    entity_type VARCHAR(50) NOT NULL,      -- 'user', 'message', 'file'
    entity_id UUID,                        -- ID da entidade afetada
    user_id UUID REFERENCES users(user_id),-- Quem executou (pode ser NULL para sistema)
    details JSONB,                         -- Contexto adicional
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_audit_logs_entity ON audit_logs(entity_type, entity_id);
CREATE INDEX idx_audit_logs_user ON audit_logs(user_id);
CREATE INDEX idx_audit_logs_created ON audit_logs(created_at DESC);
CREATE INDEX idx_audit_logs_event ON audit_logs(event_type);
```

#### 3.2.2 Função de Inserção (`Database.php`)

```php
public function insertAuditLog(
    string $eventType,
    string $entityType,
    ?string $entityId,
    ?string $userId,
    array $details = []
): void {
    $stmt = $this->pdo->prepare('
        INSERT INTO audit_logs (event_type, entity_type, entity_id, user_id, details)
        VALUES (:event_type, :entity_type, :entity_id, :user_id, :details)
    ');
    
    $stmt->execute([
        'event_type' => $eventType,
        'entity_type' => $entityType,
        'entity_id' => $entityId,
        'user_id' => $userId,
        'details' => json_encode($details)
    ]);
    
    $this->logger->debug('Audit log created', [
        'event_type' => $eventType,
        'entity_type' => $entityType,
        'entity_id' => $entityId
    ]);
}
```

#### 3.2.3 Uso em Operações Críticas

**Upload de arquivo (`FileController.php`)**:
```php
// Após complete upload
$this->database->insertAuditLog(
    'file.uploaded',
    'file',
    $fileId,
    $userId,
    [
        'conversation_id' => $fileInfo['conversation_id'],
        'filename' => $fileInfo['filename'],
        'file_size' => $fileInfo['file_size']
    ]
);
```

**Roteamento de mensagem (`MessageProcessor.php`)**:
```php
$this->database->insertAuditLog(
    'message.routed',
    'message',
    $messageId,
    $message['from_user_id'] ?? null,
    [
        'conversation_id' => $conversationId,
        'platform' => $platform,
        'processed_by' => 'router-worker',
        'has_file' => isset($message['file_id'])
    ]
);
```

**Registro de usuário (`AuthService.php`)**:
```php
// Após createUser
$this->database->insertAuditLog(
    'user.registered',
    'user',
    $userData['user_id'],
    $userData['user_id'],  // Usuário criou a si mesmo
    [
        'username' => $userData['username'],
        'has_email' => !empty($userData['email']),
        'has_phone' => !empty($userData['phone'])
    ]
);
```

---

## 4. Análise Crítica

### 4.1 Conformidade com Requisitos

| Sub-requisito | Status | Evidência |
|---------------|--------|-----------|
| **RF09**: Tipo `private` | ✅ | `type CHECK ('private', 'group')` |
| **RF09**: Tipo `group` | ✅ | `CreateGroup` em ConversationService |
| **RF09**: Roles owner/admin/member | ✅ | `role CHECK ('owner', 'admin', 'member')` |
| **RF09**: `last_read_at` tracking | ✅ | Coluna em `conversation_members` |
| **RF10**: Logs de auditoria | ✅ | Tabela `audit_logs` |
| **RF10**: Campos obrigatórios | ✅ | event_type, entity_type, entity_id, user_id, details, timestamp |

### 4.2 Limitações Identificadas

#### Limitação 1: Hierarquia de Roles Não Aplicada

**Problema**: Roles são armazenados mas não verificados em operações:
```php
// Qualquer membro pode enviar mensagem, não só owner/admin
// Não há verificação: "apenas admin pode remover membros"
```

**Solução**:
```php
public function canPerformAction(string $userId, string $conversationId, string $action): bool
{
    $role = $this->getMemberRole($userId, $conversationId);
    
    $permissions = [
        'send_message' => ['owner', 'admin', 'member'],
        'add_member' => ['owner', 'admin'],
        'remove_member' => ['owner', 'admin'],
        'delete_group' => ['owner']
    ];
    
    return in_array($role, $permissions[$action] ?? []);
}
```

#### Limitação 2: Auditoria Não Cobrindo Todas as Operações

**Problema**: Apenas algumas operações geram audit log:
- ✅ `file.uploaded`
- ✅ `message.routed`
- ❌ `user.login`
- ❌ `conversation.created`
- ❌ `member.added`

**Solução**: Adicionar logs em todas as operações críticas.

#### Limitação 3: Query N+1 em Listagem de Conversas

**Problema** (`ConversationService.php:145-170`):
```php
foreach ($conversationsData as $data) {
    // Para cada conversa, faz query adicional
    $fullConv = $this->database->getConversationById($data['conversation_id']);
}
```

**Solução**: Subquery na listagem original:
```sql
SELECT c.*, 
    (SELECT username FROM users u 
     JOIN conversation_members cm2 ON u.user_id = cm2.user_id 
     WHERE cm2.conversation_id = c.conversation_id 
     AND cm2.user_id != :user_id 
     LIMIT 1) as other_username
FROM conversations c
-- ...
```

### 4.3 Pontos Fortes

1. **JSONB para details**: Flexibilidade para contexto variado
2. **Índices otimizados**: Queries por entity, user e timestamp
3. **Desnormalização de last_message**: Listagem rápida sem JOIN
4. **Idempotência**: Não cria conversa duplicada

---

## Referências Teóricas

- **Audit Trail Pattern**: Registro imutável de operações
- **JSONB vs JSON** (PostgreSQL): Binary storage para queries eficientes
- **Denormalization**: Trade-off espaço vs performance
